# Vancouver Wellness Radar Handoff

This package contains:

- `AGENT_SPEC.md` — full repository build specification
- `CLAUDE.md` — agent manifest to commit at repo root
- `github_issues.csv` — issue backlog for import or manual creation

Recommended repository: `P-U-C/wellness-radar`.
